#include<iostream>
// #include<algorithm>
using namespace std;

int main(){
    int a,b,c;
    cout<<"enter the numbers :";
    cin>>a>>b>>c;
// using built-in functions
    // cout<<max(max(a,b),c);
// using else-if conditional statements
    // if(a>b && a>c){
    //     cout<<c<<" is greatest";
    // }
    // else if(b>a &&b>c){
    //     cout<<b<<" is greatest";
    // }
    // else{
    //     cout<<c<<" is greatest";
    // }
// using ternary operators
    //  int max=(a > b) ? ((a > c) ? a : c) : ((b > c) ? b : c);
// using initializer list
// int max= max({a,b,c}); //#include<algorithm>


}